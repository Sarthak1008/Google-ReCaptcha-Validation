package com.example.Google.ReCaptcha.Validation;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class GoogleReCaptchaValidationApplication {

	/*
	 * Step 1.) Add the following dependencies
	 * Spring Web.
	 * Thymeleaf.
	 * Spring Data JPA.
	 * MySQL Driver.
	 */

	/*
	 * Step 2.) Then we configure the properties in application.properties
	 */

	public static void main(String[] args) {
		SpringApplication.run(GoogleReCaptchaValidationApplication.class, args);
	}
	/*
	 * Step 7.) Create an Account on Google ReCAPTHA.
	 */

}
